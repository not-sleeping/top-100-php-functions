<?php

/**
 * count(array): int
 * returns amount of items in an array
 */

$inputCount = ['how', 'much', 'items', 'to', 'count'];

$output = count($inputCount);

var_dump($output);

// this returns 5

?>